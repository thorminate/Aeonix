type TNotificationCategory = "quest" | "event" | "general" | "warning";

export default TNotificationCategory;

export enum NotificationCategory {
  Quest = "quest",
  Event = "event",
  General = "general",
  Warning = "warning",
}
